{
  "name": "jarvis-desktop-relay",
  "version": "0.1.0",
  "description": "Laptop-side relay for Jarvis — translates phone tasks into Claude Code CLI invocations",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
